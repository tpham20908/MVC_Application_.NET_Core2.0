@model List<WebMVCDemo.Models.Employee>

@{
    ViewBag.Title = "Employee List";
}

<h2>Employee List</h2>

<ul>
    @foreach (var employee in Model)
    {
        <li>
            @Html.ActionLink(employee.FirstName, "Details", new { id = employee.EmployeeId})
        </li>
    }
</ul>